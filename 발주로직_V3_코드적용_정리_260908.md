# 발주분석 V3 - 웹사이트 코드에 실제 적용된 로직 정리

- 작성일: 2026-09-08
- 근거 코드: `ESM_SCM10_old/ESM_SCM8` 저장소 (문서 예시가 아닌 실제 구현 기준)
- 로직 버전: `v3.32-pl-rail-both-scenarios` (결과 스키마 버전 25)
- 핵심 파일
  - 순수 계산 엔진: `core/order_logic_v3.py`
  - 웹 워크플로 경계(법인 정책·Z행렬): `backend/services/order_logic_v3_service.py`
  - 원천 데이터 어댑터(수불·재고·리드타임): `backend/services/order_logic_v3_source.py`
  - 시즌팩터 월간 생성/활성 artifact: `backend/services/order_logic_v3_season_factor_service.py`
  - 현지통화 금액: `core/order_logic_v3_amount.py`

## 1. 전체 계산 흐름

```
완료 일별 판매 -> 13개 완료주(월~일) 구성 -> 계절 제거(deseasonalise)
-> 수요패턴 분류 -> SES / HOLT+감쇠 / Croston+SBA / 이동평균 예측
-> 미래 계절 재적용(F1/F2/F_LR) -> Layer 1·2·3 -> 목표재고 -> 원시 발주수량
-> 발주단위 올림(아웃박스 -> 인박스 -> 10개)
```

- 적용 대상 법인: `HQ`, `PL`, `USA`만 허용. 그 외 법인은 오류.
- 미확정 산식·팩터는 1.0 대체나 임의 fallback 없이 **계산 차단(fail-closed)** 이 원칙.

## 2. 관측 윈도우와 판매 원천

- 분석 기준일에서 진행 중인 주를 제외한 **직전 일요일**을 마지막 완료주 종료일로 사용.
- 그로부터 **7일 x 13 = 91일** 완료주 13개(월~일)를 구성.
- 판매 원천: 수불 `OUT-SALE`, `OUT-SALE (ONLINE)`의 **양수 출고수량**만 사용. 재고조정·이동·입고·반품 등 나머지 수불구분은 제외.
- 원본 주별 판매와 계절 제거된 주별 판매를 **둘 다 보존**해 화면/API에서 계절 제거 과정을 추적 가능하게 유지.
- 계절 제거는 주 단위가 아니라 **일별 판매를 해당 일의 달력월 팩터로 나눈 뒤** 7일 합산 (월 경계를 걸치는 주도 정확히 처리).

## 3. 수요패턴 분류 (13개 계절조정 주 기준)

판정 순서와 파라미터(textbook 후보 파라미터, 코드에 명시):

| 파라미터 | 값 |
|---|---|
| ADI 임계값 | 1.32 |
| CV² 임계값 | 0.49 |
| 추세 임계값 | ±0.20 |
| alpha / beta / phi | 0.20 / 0.10 / 0.90 |

1. **전체 0 판매** -> 승인된 신규/무판매 정책이 없으므로 차단(`DEMAND_HISTORY_INSUFFICIENT`).
2. **짧은 이력(SHORT_HISTORY)**: 윈도우 내 첫 판매주가 1주차 이후에 시작하면 SES/HOLT/Croston 게이트를 건너뛰고 **단순 이동평균** 사용 (2026-09-08 승인).
3. **간헐 수요(INTERMITTENT)**: `ADI >= 1.32` **그리고** `CV² >= 0.49` 둘 다 성립할 때만 Croston+SBA (2026-09-02 확정 로직표 B, 포함 부등호).
4. **추세 판정**: 1~6주 합 vs 8~13주 합 비교(7주차 제외). `(최근6 - 이전6) / 이전6`이
   - `> +0.20` -> TREND_UP, HOLT+감쇠
   - `< -0.20` -> TREND_DOWN, HOLT+감쇠
   - 그 외 -> STABLE, SES
- CV²는 **모집단 표준편차(pstdev)** 기반. `high_cv2`(CV² > 0.49)는 감사 플래그로만 유지하고 sigma 보강 산식은 미승인 상태로 미적용.
- 이전 6주 합이 0이면 추세 신호가 정의되지 않아 차단.

## 4. 예측 엔진

### SES (STABLE)
- 초기 level = 1주차 값, `level += alpha * (관측 - level)` 재귀.
- sigma = 1주 앞 예측오차 RMSE. 다음주/다다음주 예측 = level.

### HOLT + 감쇠 (TREND_UP/DOWN)
- 초기화 = **6주 최소제곱 회귀** (2026-09-08 승인, 초기추세오류 오답노트 반영):
  - `b0 = (-5*Y1 - 3*Y2 - Y3 + Y4 + 3*Y5 + 5*Y6) / 35`
  - `l0 = mean(Y1..Y6) + b0 * 2.5`
  - 재귀는 7~13주차만 소비 (관측 중복 사용 없음).
- 감쇠 예측: `F(h) = level + trend * phi * (1 - phi^h) / (1 - phi)`.
- 리드타임 구간 수요는 주 단위 예측을 **소수 주 단위까지 구간 적분**해 합산.

### Croston + SBA (INTERMITTENT)
- 초기화 = 첫 간격/첫 수요량. 비영(0이 아닌) 판매주가 2개 미만이면 차단.
- `demand = (size / interval) * (1 - alpha/2)` (SBA 보정).
- 분산 = `size_sigma²/interval + size² * (1/interval) * (1 - 1/interval)`.

### 이동평균 (SHORT_HISTORY)
- 첫 판매주부터 마지막 완료주까지(중간 0판매주 포함) 단순 평균 = 수요, 표본 표준편차(STDEV.S) = sigma.
- 판매 이후 완료주가 2개 미만이면 차단.

### 신규 SKU 상태 (감사용)
- 첫 판매일~분석일 **달력일수 168일 이하**면 신규 SKU (168일 정확히 포함). 계산에는 개입하지 않는 감사 데이터.

## 5. 시즌팩터 (계절지수)

- **월간 프로파일**: 직전 **24개 완료 달력월** 판매로 산출. 오래된 12개월 가중 1, 최근 12개월 가중 2. 월 팩터 = (가중 월평균) / (가중 전체평균).
- **저표본 축소(shrinkage)**: `w = n / (n + 30)`, `최종 = w*조합팩터 + (1-w)*상위팩터`. 계절성 미확인 그룹은 1.0.
- **적용 스코프 사다리**: 법인 + 기능구분1·2 조합 팩터 -> 기능구분2 미분류 시 기능구분1 팩터 -> 그것도 없으면 **차단** (`SEASON_FACTOR_MISSING` / `SEASON_FACTOR_CALC_FAILED` / `SEASON_FACTOR_MAPPING_MISSING`). 1.0 대체 없음.
- **활성 artifact 필수**: 검증 통과 후 자동 활성화된 24개월 artifact가 없으면 계산 차단(`SEASON_FACTOR_ARTIFACT_NOT_ACTIVE`). 수불 판매 기준(`LEDGER_POLICY`)이 아닌 artifact도 차단.
- **미래 계절 재적용 (SF-002)**: 실제 달력 날짜로 가중 평균
  - `F1` = 발주일~리드타임 종료 구간 팩터
  - `F2` = 리드타임 종료~검토주기 종료 구간 팩터
  - `F_LR` = 발주일~검토주기 종료 전체 구간 팩터
  - 월 경계를 걸치는 소수 일수는 그 달에 그대로 귀속 (반올림 없음).

## 6. Layer 구조와 발주수량

- 재고위치(IP) = `현재고 + 상위창고 가용 + 운송중 + 미입고 - 보류(holding)`.
- **Layer 1** (리드타임 수요) = 주당 수요 x 리드타임(주) x F1. HOLT는 구간 적분 x F1.
- **Layer 2** (안전재고) = `Z * sqrt((L+R주) * (sigma*F_LR)² + (주당수요*F_LR)² * sigma_LT²)`
  - 하한/상한 펜스: `주당수요 x 펜스주수`로 clamp.
  - 펜스(주): **PL 2~10, USA 2~8, HQ 1~2**.
- **Layer 3** (검토주기 수요) = 주당 수요 x 검토주기(주) x F2. HOLT는 구간 적분 x F2.
- ROP = Layer1 + Layer2 (참고용 표시값, 발주 게이트 아님).
- **목표재고(S) = Layer1 + Layer2 + Layer3**, 순수 정기보충 (R,S).
- **원시 발주수량 = max(0, 목표재고 - IP)**.
- **발주단위 올림 사다리** (2026-09-08 승인): 아웃박스 팩 -> 인박스 팩 -> 고정 10개. `ceil(raw/unit)*unit`. 원시수량 0이면 올림으로 발주를 만들지 않음.
- 현지통화 금액 = 발주수량 x 현지 단가 (FX 없음, 누락 입력은 null 유지).

## 7. Z값 정책 (법인별, 2026-09-02 승인)

두 시나리오(SHORTAGE 결품방지 / CASH 현금흐름) x 판매등급(CORE 주력 / GENERAL 일반):

| 법인 | 정책 | SHORTAGE CORE | SHORTAGE GENERAL | CASH CORE | CASH GENERAL |
|---|---|---|---|---|---|
| HQ | 서비스레벨 80/70/60/50 | 0.8416 | 0.5244 | 0.2533 | **0.0** |
| PL·USA | 서비스레벨 90/80/70/60 | 1.2816 | 0.8416 | 0.5244 | 0.2533 |
| (참고) textbook | 기본 행렬 | 1.68 | 1.28 | 1.28 | 1.08 |

- HQ CASH/GENERAL은 의도적으로 Z=0 (SS_raw=0 후 HQ 1주 펜스 하한 적용).
- 두 시나리오는 동일한 판매 윈도우·계절 프로파일·분류·예측·재고 스냅샷을 공유하고, Z값과 리드타임/검토주기 정책만 다름.
- 판매등급(CORE/GENERAL)의 SKU별 원천·판정 규칙은 **미확정 차단 계약**(`SALES_GRADE_SOURCE_PENDING`)으로 남아 있음.

## 8. 리드타임·검토주기·운송모드

- 검토주기(R) = **28일** 고정.
- 리드타임: 최근 **6개 완료월** 패킹 실측치의 평균/표준편차. **표본 10건 미만이면 차단**. sigma_LT는 일 기준 표준편차 / 7 (주 단위 환산).
- 운송모드 (2026-09-08 사용자 결정 반영):
  - **PL: CASH·SHORTAGE 모두 RAIL** (SEA는 운송중 ETA 계산용으로만 계속 실측).
  - **USA: CASH=AIR, SHORTAGE=SEA**.

## 9. 재고위치 원천 정책 (V2 어댑터 값)

- PL/USA: `현지 가용 + 본사(EU) 가용 + 운송중 + 미입고`. V2 `avbl_qty` 신뢰 (별도 TROUBLE 필터 없음).
- HQ: `현지 가용 + 미입고` (EU 가용·운송중 = 0). V2 어댑터에서 `stock_status=trouble` 제외.
- 미입고 = ① 오픈PO + ② PNFM 확정 + ③ 입고진행중. 입고완료분 제외.
- **① 오픈PO만 최근 30일 내 PO 등록분으로 제한** (PL/USA는 별도 쿼리, HQ는 오래된 행 0 처리). ②③은 이미 입고 절차에 들어간 물량이라 전체 윈도우 유지.
- V2 재고 검증 오류는 해당 SKU 계산을 차단, 경고는 수량을 바꾸지 않고 표시만.

## 10. SKU 범위 예외

- 사용자 승인 예외 (2026-08-31, `v3-sku-scope-2026-08-31`): USA에서 `IUS04-CEU`, `JSMS01-SgEU`, `MECUS08-TPREU` 제외. 제품명 규칙이 아닌 명시적 식별자 목록이며 감사 로그에 SHA-256 지문으로 기록.

## 11. 의도적으로 아직 미적용(차단 상태)인 것

- **MOQ·팔레트·자동 발주 확정**: 원시수량 단계(V3-012)라 제외. 상태 코드 `ORDER_CONSTRAINTS_PENDING` (팩 올림 적용 시 `PACK_ROUNDING_APPLIED`).
- **CV² > 0.49 sigma 보강 산식**: 미승인. `high_cv2` 감사 플래그만 노출.
- **판매등급 원천 규칙**: Z행렬은 확정, SKU별 등급 판정은 미확정.
- **V2 비교 백테스트 외 운영 개입**: 롤링 오리진 백테스트는 예측 전용(재고·팩·MOQ 제외).

## 12. 실패 처리 원칙 요약

| 상황 | 처리 |
|---|---|
| 활성 시즌팩터 artifact 없음 | 전체 차단, 1.0 대체 금지 |
| 기능구분 미분류/팩터 없음 | SKU 차단 + 사유코드 |
| 전체 0 판매 / 비영 판매 부족 | SKU 차단 (`DEMAND_HISTORY_INSUFFICIENT`) |
| 리드타임 표본 < 10 | 차단 |
| 재고 음수 등 V2 검증 오류 | SKU 차단 |
| 미승인 정책 필요 | `V3PolicyRequiredError`로 차단, 임의 기본값 금지 |
